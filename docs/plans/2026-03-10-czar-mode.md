# Czar Mode & Czar is Dead Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Přidat nové pole `czarMode` do nastavení místnosti (Classic / Meritocracy / God Mode / Czar is Dead) a implementovat pravidlo "Czar is Dead" — hlasování všech hráčů místo rozhodnutí jednoho czara.

**Architecture:** `czarMode: CzarMode` nahrazuje `god_mode` a `meritocracy` ze `SpecialRule[]`. Backend `GameEngine` nově přijímá `czarMode` a v módu `czar_is_dead` se nevolí czar — všichni hráči odevzdají karty a pak hlasují. Hlasování sleduje `GameEngine` (nová mapa `votes`), výsledek vyhodnocuje `resolveVoting()` v `roundUtils.ts`.

**Tech Stack:** TypeScript, Fastify, Socket.io, Vue 3, Pinia, Vitest, Zod

---

## Důležité cesty

- `packages/shared/src/index.ts` — typy
- `packages/backend/src/game/GameEngine.ts` — herní logika
- `packages/backend/src/game/RoomManager.ts` — správa místností
- `packages/backend/src/socket/roundUtils.ts` — přechody fází
- `packages/backend/src/socket/gameHandlers.ts` — socket handlery
- `packages/backend/src/socket/validation.ts` — Zod schémata
- `packages/backend/src/socket/lobbyHandlers.ts` — vytvoření místnosti
- `packages/frontend/src/i18n/locales/cs.json` — překlady (fallback pro všechny jazyky)
- `packages/frontend/src/components/SpecialRulesPanel.vue`
- `packages/frontend/src/components/CreateTableModal.vue`
- `packages/frontend/src/components/RoomSettingsModal.vue`
- `packages/frontend/src/components/JudgingPhase.vue`
- `packages/frontend/src/stores/roomStore.ts`

---

## Task 1: Typy v @kpl/shared

**Files:**
- Modify: `packages/shared/src/index.ts`

### Step 1: Přidej `CzarMode` typ a odeber `god_mode`/`meritocracy` ze `SpecialRule`

```typescript
// Nahraď stávající SpecialRule — odeber 'god_mode' a 'meritocracy'
export type SpecialRule =
  | 'rando_cardrissian'
  | 'wheatons_law'
  | 'rebooting_universe'
  | 'high_stakes';

// Přidej nový typ
export type CzarMode = 'classic' | 'meritocracy' | 'god_mode' | 'czar_is_dead';
```

### Step 2: Přidej `czarMode` do `GameRoom`

```typescript
// V interface GameRoom přidej (za specialRules):
czarMode: CzarMode;  // výchozí: 'classic'
```

### Step 3: Aktualizuj `RoundResult` pro multi-winner

```typescript
// V interface RoundResult přidej:
winnerIds?: string[];  // czar_is_dead: více vítězů (remíza)
```

### Step 4: Přidej `czarMode` do `GameRoundStart`

```typescript
// V interface GameRoundStart přidej:
czarMode: CzarMode;
```

### Step 5: Přidej nové socket eventy

```typescript
// Do ServerToClientEvents přidej:
'game:voteUpdate': (data: { votedCount: number; totalVoters: number }) => void;

// Do ClientToServerEvents přidej:
'game:vote': (submissionId: string) => void;
'game:skipVoting': () => void;
```

### Step 6: Aktualizuj `lobby:create` a `lobby:updateSettings` v ClientToServerEvents

```typescript
// V 'lobby:create' settings přidej:
czarMode?: CzarMode;

// V 'lobby:updateSettings' settings přidej:
czarMode?: CzarMode;
```

### Step 7: Spusť build shared

```bash
npm run build --workspace=packages/shared
```
Očekávaný výstup: žádné TS chyby (backend/frontend budou mít dočasně chyby kvůli odstraněným typům).

---

## Task 2: Backend — GameEngine

**Files:**
- Modify: `packages/backend/src/game/GameEngine.ts`
- Test: `packages/backend/src/game/GameEngine.test.ts`

### Step 1: Napiš failing testy pro czarMode

Přidej do `GameEngine.test.ts` nový `describe` blok:

```typescript
describe('czarMode', () => {
  it('classic: rotates czar as usual', () => {
    const e = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'classic');
    e.startRound();
    expect(players.filter(p => p.isCardCzar)).toHaveLength(1);
  });

  it('god_mode: host is always czar', () => {
    const e = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'god_mode');
    e.startRound();
    expect(players.find(p => p.id === 'p1')?.isCardCzar).toBe(true);
    e.startRound();
    expect(players.find(p => p.id === 'p1')?.isCardCzar).toBe(true);
  });

  it('meritocracy: winner of last round becomes czar', () => {
    const e = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'meritocracy');
    e.startRound();
    // set last round winner manually
    (e as any).lastRoundWinnerId = 'p2';
    e.startRound();
    expect(players.find(p => p.id === 'p2')?.isCardCzar).toBe(true);
  });

  it('czar_is_dead: no player gets isCardCzar=true', () => {
    const e = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'czar_is_dead');
    e.startRound();
    expect(players.filter(p => p.isCardCzar)).toHaveLength(0);
  });

  it('czar_is_dead: all players can submit cards', () => {
    const e = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'czar_is_dead');
    e.startRound();
    for (const p of players) {
      const cardId = e.getPlayerHand(p.id)[0].id;
      const result = e.submitCards(p.id, [cardId]);
      expect(result).toEqual({ ok: true, allSubmitted: expect.any(Boolean) });
    }
  });
});

describe('czar_is_dead voting', () => {
  let engine: GameEngine;

  beforeEach(() => {
    engine = new GameEngine(players, makeBlackCards(20), makeWhiteCards(100), [], 'p1', 'czar_is_dead');
    engine.startRound();
    // submit cards for all players
    for (const p of players) {
      const cardId = engine.getPlayerHand(p.id)[0].id;
      engine.submitCards(p.id, [cardId]);
    }
  });

  it('castVote: player can vote for another submission', () => {
    const subs = engine.getAnonymousSubmissions();
    const subNotOwned = subs.find(s => {
      // find submission not owned by p1
      return (engine as any).submissions.get('p1')?.submissionId !== s.submissionId;
    })!;
    const result = engine.castVote('p1', subNotOwned.submissionId);
    expect(result).toEqual({ ok: true, allVoted: false });
  });

  it('castVote: player cannot vote for own submission', () => {
    const ownSub = (engine as any).submissions.get('p1');
    const result = engine.castVote('p1', ownSub.submissionId);
    expect('error' in result).toBe(true);
  });

  it('resolveVotes: winner has most votes', () => {
    const subs = engine.getAnonymousSubmissions();
    // p1 a p2 votes for p3's submission
    const p3Sub = (engine as any).submissions.get('p3');
    engine.castVote('p1', p3Sub.submissionId);
    engine.castVote('p2', p3Sub.submissionId);
    const result = engine.resolveVotes();
    expect(result.winnerIds).toEqual(['p3']);
    const p3 = players.find(p => p.id === 'p3')!;
    expect(p3.score).toBe(1);
  });

  it('resolveVotes: tie gives all tied players +1', () => {
    const subs = engine.getAnonymousSubmissions();
    const p2Sub = (engine as any).submissions.get('p2');
    const p3Sub = (engine as any).submissions.get('p3');
    engine.castVote('p1', p2Sub.submissionId);
    engine.castVote('p2', p3Sub.submissionId);
    engine.castVote('p3', p2Sub.submissionId); // p2 má 2 hlasy ale p3 má 1 - wait, jen 2 hlasy celkem
    // Let me redo: p1→p2, p2→p3 -> p2 has 1, p3 has 1 -> TIE between p2 and p3
    // Reset votes
    (engine as any).votes.clear();
    engine.castVote('p1', p2Sub.submissionId);
    engine.castVote('p3', p3Sub.submissionId);
    const result = engine.resolveVotes();
    expect(result.winnerIds).toHaveLength(2);
    expect(result.winnerIds).toContain('p2');
    expect(result.winnerIds).toContain('p3');
    const p2 = players.find(p => p.id === 'p2')!;
    const p3 = players.find(p => p.id === 'p3')!;
    expect(p2.score).toBe(1);
    expect(p3.score).toBe(1);
  });

  it('resolveVotes: no votes → no winner, winnerIds empty', () => {
    const result = engine.resolveVotes();
    expect(result.winnerIds).toEqual([]);
    expect(result.winnerId).toBeNull();
  });
});
```

### Step 2: Spusť testy — ověř že FAILují

```bash
npm test --workspace=packages/backend -- --reporter=verbose 2>&1 | grep -E "(FAIL|PASS|czarMode|voting)"
```

### Step 3: Implementuj změny v `GameEngine.ts`

**3a: Přidej `czarMode` do konstruktoru a `EngineSnapshot`**

```typescript
// EngineSnapshot — přidej pole:
czarMode: CzarMode;
votes: Record<string, string>;  // playerId → submissionId

// Konstruktor — přidej parametr:
constructor(
  private players: Player[],
  blackCards: BlackCard[],
  whiteCards: WhiteCard[],
  specialRules: SpecialRule[] = [],
  hostId: string = '',
  private czarMode: CzarMode = 'classic',
)
```

**3b: Přidej import `CzarMode` v záhlaví souboru**

```typescript
import type { BlackCard, WhiteCard, Player, AnonymousSubmission, RoundResult, SpecialRule, CzarMode } from '@kpl/shared';
```

**3c: Přidej `votes` mapu jako privátní pole**

```typescript
private votes = new Map<string, string>(); // playerId → submissionId
```

**3d: Vymaž `votes` na začátku `startRound()`**

```typescript
// Za this.bets.clear(); přidej:
this.votes.clear();
```

**3e: Uprav `startRound()` — v czar_is_dead žádný czar**

```typescript
// Nahraď:
const czar = this.pickNextCzar();
czar.isCardCzar = true;

// Za:
let czarId: string;
if (this.czarMode === 'czar_is_dead') {
  czarId = '';  // žádný czar
} else {
  const czar = this.pickNextCzar();
  czar.isCardCzar = true;
  czarId = czar.id;
}
```

**3f: Uprav `pickNextCzar()` — použij `czarMode` místo specialRules**

```typescript
private pickNextCzar(): Player {
  // god_mode: host is always czar
  if (this.czarMode === 'god_mode') {
    const host = this.players.find(p => p.id === this.hostId && !p.isAfk);
    if (host) { this.czarPointer = this.players.indexOf(host); return host; }
  }
  // meritocracy: winner of last round becomes czar
  if (this.czarMode === 'meritocracy' && this.lastRoundWinnerId) {
    const winner = this.players.find(p => p.id === this.lastRoundWinnerId && !p.isAfk);
    if (winner) { this.czarPointer = this.players.indexOf(winner); return winner; }
  }
  // default rotation
  const n = this.players.length;
  for (let i = 1; i <= n; i++) {
    const idx = (this.czarPointer + i) % n;
    if (!this.players[idx].isAfk) {
      this.czarPointer = idx;
      return this.players[idx];
    }
  }
  throw new Error('Žádní aktivní hráči.');
}
```

**3g: Uprav `submitCards()` — v czar_is_dead mohou hrát všichni**

```typescript
// Nahraď kontrolu czara:
if (player.isCardCzar) return { error: 'Card Czar nemůže hrát karty.' };

// Za (zachovej jen pro non-czar_is_dead):
if (player.isCardCzar) return { error: 'Card Czar nemůže hrát karty.' };

// A v allSubmitted logice uprav:
const nonCzarActive = this.czarMode === 'czar_is_dead'
  ? this.players.filter(p => !p.isAfk)
  : this.players.filter(p => !p.isAfk && !p.isCardCzar);
const allSubmitted = nonCzarActive.every(p => p.hasPlayed);
```

**3h: Přidej `castVote()` metodu**

```typescript
castVote(
  playerId: string,
  submissionId: string,
): { ok: true; allVoted: boolean } | { error: string } {
  const player = this.players.find(p => p.id === playerId);
  if (!player) return { error: 'Hráč nenalezen.' };
  if (player.isAfk) return { error: 'AFK hráč nemůže hlasovat.' };

  // Zkontroluj, že hráč nevolí vlastní submission
  const ownSub = this.submissions.get(playerId);
  if (ownSub && ownSub.submissionId === submissionId) {
    return { error: 'Nemůžeš hlasovat pro vlastní karty.' };
  }

  // Ověř, že submissionId existuje
  const validIds = new Set([
    ...Array.from(this.submissions.values()).map(s => s.submissionId),
    ...(this.randoSubmission ? [this.randoSubmission.submissionId] : []),
  ]);
  if (!validIds.has(submissionId)) return { error: 'Neplatné ID submise.' };

  this.votes.set(playerId, submissionId);

  const activePlayers = this.players.filter(p => !p.isAfk);
  const allVoted = activePlayers.every(p => this.votes.has(p.id));
  return { ok: true, allVoted };
}
```

**3i: Přidej `resolveVotes()` metodu**

```typescript
resolveVotes(): RoundResult {
  // Počet hlasů per submissionId
  const voteCounts = new Map<string, number>();
  for (const submissionId of this.votes.values()) {
    voteCounts.set(submissionId, (voteCounts.get(submissionId) ?? 0) + 1);
  }

  const maxVotes = Math.max(0, ...voteCounts.values());

  if (maxVotes === 0) {
    const scores: Record<string, number> = {};
    for (const p of this.players) scores[p.id] = p.score;
    return { winnerId: null, winnerNickname: null, winningCards: [], scores, winnerIds: [] };
  }

  // Najdi submissionId(s) s maximem hlasů
  const winningSubmissionIds = Array.from(voteCounts.entries())
    .filter(([, count]) => count === maxVotes)
    .map(([sid]) => sid);

  // Mapuj submissionId → playerId
  const subToPlayer = new Map<string, string>();
  for (const [pid, sub] of this.submissions.entries()) {
    subToPlayer.set(sub.submissionId, pid);
  }

  const winnerIds: string[] = [];
  let winningCards: WhiteCard[] = [];

  for (const subId of winningSubmissionIds) {
    const pid = subToPlayer.get(subId);
    if (pid) {
      winnerIds.push(pid);
      const winner = this.players.find(p => p.id === pid)!;
      winner.score++;
      if (winningCards.length === 0) {
        winningCards = this.submissions.get(pid)!.cards;
      }
    }
    // Rando win
    if (this.randoSubmission && subId === this.randoSubmission.submissionId) {
      winnerIds.push(GameEngine.RANDO_ID);
      if (winningCards.length === 0) winningCards = this.randoSubmission.cards;
    }
  }

  const scores: Record<string, number> = {};
  for (const p of this.players) scores[p.id] = p.score;

  // lastRoundWinnerId: první lidský vítěz (pro meritocracy v příštím kole)
  const firstHuman = winnerIds.find(id => id !== GameEngine.RANDO_ID);
  if (firstHuman) this.lastRoundWinnerId = firstHuman;

  const firstWinnerId = winnerIds[0] ?? null;
  const firstWinnerNickname = firstWinnerId === GameEngine.RANDO_ID
    ? 'Rando Cardrissian'
    : (this.players.find(p => p.id === firstWinnerId)?.nickname ?? null);

  return {
    winnerId: firstWinnerId,
    winnerNickname: firstWinnerNickname,
    winningCards,
    scores,
    winnerIds,
  };
}
```

**3j: Přidej `getCzarMode()` getter**

```typescript
getCzarMode(): CzarMode {
  return this.czarMode;
}
```

**3k: Uprav `startRound()` return type — vrať `czarId: string | null`**

```typescript
// Nahraď return type a return hodnoty:
startRound(): { czarId: string | null; blackCardCandidates?: BlackCard[] } {
  // ...
  // V czar_is_dead větvi:
  if (this.czarMode === 'czar_is_dead') {
    // Rando cardrissian: stále přidej submission
    // ...
    this.currentBlackCard = blackCard;
    this.blackCardCandidates = null;
    return { czarId: null };
  }
  // ...
  return { czarId: czar.id };
  // Wheaton's Law branch:
  return { czarId: czar.id, blackCardCandidates: this.blackCardCandidates };
}
```

**3l: Uprav `toSnapshot()` a `fromSnapshot()` pro nová pole**

```typescript
// toSnapshot() — přidej:
czarMode: this.czarMode,
votes: Object.fromEntries(this.votes),

// fromSnapshot() — přidej:
engine.czarMode = snap.czarMode ?? 'classic';
engine.votes = new Map(Object.entries(snap.votes ?? {}));
```

### Step 4: Spusť testy

```bash
npm test --workspace=packages/backend
```
Očekávaný výstup: nové testy PASují, stávající testy PASují.

### Step 5: Commit

```bash
git add packages/shared/src/index.ts packages/backend/src/game/GameEngine.ts packages/backend/src/game/GameEngine.test.ts
git commit -m "feat: add CzarMode type and czar_is_dead voting logic to GameEngine"
```

---

## Task 3: Backend — RoomManager a validation

**Files:**
- Modify: `packages/backend/src/game/RoomManager.ts`
- Modify: `packages/backend/src/socket/validation.ts`
- Modify: `packages/backend/src/socket/lobbyHandlers.ts`

### Step 1: Napiš failing testy do `RoomManager.test.ts`

```typescript
it('createRoom: stores czarMode in room', () => {
  const result = manager.createRoom({
    ...baseSettings,
    czarMode: 'god_mode',
  });
  expect(result.room.czarMode).toBe('god_mode');
});

it('createRoom: default czarMode is classic', () => {
  const result = manager.createRoom(baseSettings);
  expect(result.room.czarMode).toBe('classic');
});

it('updateSettings: can change czarMode', () => {
  const { room, playerToken } = manager.createRoom(baseSettings);
  manager.updateSettings(playerToken, { czarMode: 'meritocracy' });
  expect(room.czarMode).toBe('meritocracy');
});
```

### Step 2: Spusť testy — ověř FAIL

```bash
npm test --workspace=packages/backend -- --reporter=verbose 2>&1 | grep -E "czarMode"
```

### Step 3: Uprav `RoomManager.ts`

**3a: Přidej `CzarMode` import**

```typescript
import type { GameRoom, GameOverPayload, Player, PublicRoomSummary, SpecialRule, WinCondition, CzarMode } from '@kpl/shared';
```

**3b: Přidej `czarMode` do `CreateRoomSettings`**

```typescript
export interface CreateRoomSettings {
  // ...stávající pole...
  czarMode?: CzarMode;
}
```

**3c: Přidej `czarMode` do `UpdateSettingsData`**

```typescript
export interface UpdateSettingsData {
  // ...stávající pole...
  czarMode?: CzarMode;
}
```

**3d: Přidej `czarMode` do `createRoom()` inicializace místnosti**

```typescript
const room: GameRoom = {
  // ...stávající pole...
  czarMode: settings.czarMode ?? 'classic',
};
```

**3e: Přidej `czarMode` update do `updateSettings()`**

```typescript
if (settings.czarMode !== undefined) room.czarMode = settings.czarMode;
```

**3f: Přidej `czarMode` do `GameEngine` konstruktoru v `startGame()`**

Najdi kde se `GameEngine` konstruuje (v `lobbyHandlers.ts`) a přidej `czarMode`. Viz Task 3, Step 5.

### Step 4: Uprav `validation.ts`

**4a: Přidej `VALID_CZAR_MODES` a `czarMode` schéma**

```typescript
import type { SpecialRule, WinCondition, CzarMode } from '@kpl/shared';

const VALID_CZAR_MODES: CzarMode[] = ['classic', 'meritocracy', 'god_mode', 'czar_is_dead'];
const czarMode = z.enum(VALID_CZAR_MODES as [CzarMode, ...CzarMode[]]).default('classic');
```

**4b: Odeber `god_mode` a `meritocracy` z `VALID_RULES`**

```typescript
const VALID_RULES: SpecialRule[] = [
  'rando_cardrissian', 'wheatons_law',
  'rebooting_universe', 'high_stakes',
];
```

**4c: Přidej `czarMode` do `CreateRoomSchema` a `UpdateSettingsSchema`**

```typescript
// CreateRoomSchema:
czarMode: czarMode,

// UpdateSettingsSchema:
czarMode: czarMode.optional(),
```

**4d: Přidej `VoteSchema`**

```typescript
export const VoteSchema = z.union([
  z.string().uuid(),
  z.literal('rando_cardrissian'),
]);
```

### Step 5: Uprav `lobbyHandlers.ts` — předej `czarMode` do `GameEngine`

Najdi místo kde se volá `new GameEngine(...)` (nebo kde se spouští hra). Přidej `czarMode`:

```typescript
const engine = new GameEngine(
  room.players,
  blackCards,
  whiteCards,
  room.specialRules,
  room.hostId,
  room.czarMode ?? 'classic',
);
```

### Step 6: Spusť testy

```bash
npm test --workspace=packages/backend
```
Všechny testy musí projít.

### Step 7: Commit

```bash
git add packages/backend/src/game/RoomManager.ts packages/backend/src/socket/validation.ts packages/backend/src/socket/lobbyHandlers.ts packages/backend/src/game/RoomManager.test.ts
git commit -m "feat: add czarMode to RoomManager, validation schemas, and lobbyHandlers"
```

---

## Task 4: Backend — roundUtils a gameHandlers

**Files:**
- Modify: `packages/backend/src/socket/roundUtils.ts`
- Modify: `packages/backend/src/socket/gameHandlers.ts`

### Step 1: Uprav `roundUtils.ts` — `startJudgingPhase` pro `czar_is_dead`

Přidej `czar_is_dead` větev:

```typescript
export function startJudgingPhase(room: GameRoom, engine: GameEngine, io: IO): void {
  const roomCode = room.code;
  room.status = 'JUDGING';
  room.roundDeadline = Date.now() + JUDGING_TIMEOUT_MS;
  io.to(`room:${roomCode}`).emit('lobby:stateUpdate', toPublicRoom(room));
  io.to(`room:${roomCode}`).emit('game:judging', engine.getAnonymousSubmissions());

  // Emit initial vote count for czar_is_dead
  if (engine.getCzarMode() === 'czar_is_dead') {
    const activePlayers = room.players.filter(p => !p.isAfk);
    io.to(`room:${roomCode}`).emit('game:voteUpdate', {
      votedCount: 0,
      totalVoters: activePlayers.length,
    });
  }

  roomManager.setJudgingTimer(roomCode, () => {
    // Timer vypršel — v classic/meritocracy/god_mode čeká se na game:skipCzarJudging
    // V czar_is_dead: zobrazí se tlačítko "Dál nečekat" (deadline je v minulosti)
  }, JUDGING_TIMEOUT_MS);
}
```

### Step 2: Přidej `resolveVotingAndEnd()` helper do `roundUtils.ts`

```typescript
export function resolveVotingAndEnd(room: GameRoom, engine: GameEngine, io: IO): void {
  const roomCode = room.code;
  roomManager.clearJudgingTimer(roomCode);
  roomManager.updateActivity(roomCode);

  const result = engine.resolveVotes();

  room.status = 'RESULTS';
  room.roundDeadline = null;
  io.to(`room:${roomCode}`).emit('lobby:stateUpdate', toPublicRoom(room));
  io.to(`room:${roomCode}`).emit('game:roundEnd', result);

  // Win condition check — pro czar_is_dead: vyhraj pokud winnerId nebo winnerIds obsahuje hráče s dostatkem bodů
  const winnerIdsToCheck = result.winnerIds && result.winnerIds.length > 0
    ? result.winnerIds
    : (result.winnerId ? [result.winnerId] : []);

  const someoneWon = winnerIdsToCheck.some(wid => {
    if (wid === GameEngine.RANDO_ID) return false;
    switch (room.winCondition ?? 'score') {
      case 'score': return (result.scores[wid] ?? 0) >= room.targetScore;
      case 'rounds': return engine.roundNumber >= room.targetRounds;
      case 'time': return !!(room.gameStartedAt && Date.now() - room.gameStartedAt >= room.gameTimeLimit * 60_000);
    }
  });

  if (someoneWon || ((room.winCondition ?? 'score') !== 'score' && isRoundsOrTimeWon(room, engine))) {
    // Použij finishGame — stejně jako v gameHandlers
    // Importuj socketToToken a broadcastPublicRooms
    // Toto je sdílená logika — viz gameHandlers.ts endRound helper
  }
  // ...
}
```

> **Poznámka implementátorovi:** Logika ukončení hry (finishGame + socket cleanup) se opakuje v `gameHandlers.ts`. Nejjednodušší přístup: přesuň tuto logiku do inline funkce `finishAndBroadcast(room, io)` v `gameHandlers.ts` a zavolej ji z obou handlerů (judgeSelect i game:vote). Nebo udělej `resolveVotingAndEnd` jen jako helper co vrátí `result` a nechej celou logiku v handleru. **Doporučuji:** udělej `resolveVotingAndEnd` jako helper co vrátí `result` a celou logiku ukončení/dalšího kola nechej v handleru `game:vote`.

### Step 3: Přidej `game:vote` handler do `gameHandlers.ts`

```typescript
// Přidej import VoteSchema
import { PlayCardsSchema, JudgeSelectSchema, VoteSchema, ChooseBlackCardSchema, PlaceBetSchema, validate } from './validation.js';

// Přidej handler (za game:judgeSelect):
socket.on('game:vote', (submissionId) => {
  if (!checkRateLimit(socket.id, 'game:vote')) {
    socket.emit('game:error', 'Příliš mnoho požadavků. Zkus to za chvíli.');
    return;
  }
  const id = validate(VoteSchema, submissionId);
  if (!id) { socket.emit('game:error', 'Neplatné submissionId.'); return; }

  const playerToken = socketToToken.get(socket.id);
  if (!playerToken) return;

  const room = roomManager.getRoomByPlayerToken(playerToken);
  if (!room || room.status !== 'JUDGING') {
    socket.emit('game:error', 'Hra není ve fázi hlasování.');
    return;
  }

  const engine = roomManager.getGameEngine(room.code);
  if (!engine || engine.getCzarMode() !== 'czar_is_dead') {
    socket.emit('game:error', 'Hlasování není v tomto módu dostupné.');
    return;
  }

  const playerId = roomManager.getPlayerIdByToken(playerToken)!;
  const result = engine.castVote(playerId, id);

  if ('error' in result) {
    socket.emit('game:error', result.error);
    return;
  }

  roomManager.updateActivity(room.code);

  // Broadcast vote count update
  const activePlayers = room.players.filter(p => !p.isAfk);
  const votedCount = activePlayers.filter(p => engine.hasVoted(p.id)).length;
  io.to(`room:${room.code}`).emit('game:voteUpdate', {
    votedCount,
    totalVoters: activePlayers.length,
  });

  if (result.allVoted) {
    // Všichni hlasovali → vyhodnoť
    const roundResult = engine.resolveVotes();
    roomManager.clearJudgingTimer(room.code);

    room.status = 'RESULTS';
    room.roundDeadline = null;
    io.to(`room:${room.code}`).emit('lobby:stateUpdate', toPublicRoom(room));
    io.to(`room:${room.code}`).emit('game:roundEnd', roundResult);

    handlePostRound(room, engine, roundResult, io);
  }
});
```

### Step 4: Přidej `game:skipVoting` handler do `gameHandlers.ts`

```typescript
socket.on('game:skipVoting', () => {
  const playerToken = socketToToken.get(socket.id);
  if (!playerToken) return;

  const room = roomManager.getRoomByPlayerToken(playerToken);
  if (!room || room.status !== 'JUDGING') return;

  const engine = roomManager.getGameEngine(room.code);
  if (!engine || engine.getCzarMode() !== 'czar_is_dead') return;

  // Zkontroluj, že deadline vypršel
  if (!room.roundDeadline || Date.now() < room.roundDeadline) {
    socket.emit('game:error', 'Časový limit ještě nevypršel.');
    return;
  }

  const roundResult = engine.resolveVotes();
  roomManager.clearJudgingTimer(room.code);
  roomManager.updateActivity(room.code);

  room.status = 'RESULTS';
  room.roundDeadline = null;
  io.to(`room:${room.code}`).emit('lobby:stateUpdate', toPublicRoom(room));
  io.to(`room:${room.code}`).emit('game:roundEnd', roundResult);

  handlePostRound(room, engine, roundResult, io);
});
```

### Step 5: Refaktoruj sdílenou logiku post-round do `handlePostRound()`

Extrahuj opakující se logiku z `game:judgeSelect` do lokální funkce:

```typescript
function handlePostRound(room: GameRoom, engine: GameEngine, result: RoundResult, io: IO): void {
  if (isWinConditionMet(room, engine, result)) {
    const finishResult = roomManager.finishGame(room.code);
    if (!('error' in finishResult)) {
      io.to(`room:${room.code}`).emit('game:gameOver', finishResult.payload);
      for (const [sid, token] of socketToToken.entries()) {
        if (finishResult.kickedTokens.includes(token)) {
          const kickedSocket = io.sockets.sockets.get(sid);
          if (kickedSocket) kickedSocket.leave(`room:${room.code}`);
          socketToToken.delete(sid);
        }
      }
      io.to(`room:${room.code}`).emit('lobby:stateUpdate', toPublicRoom(finishResult.room));
      broadcastPublicRooms(io);
    }
    return;
  }

  const roomCode = room.code;
  setTimeout(() => {
    const currentRoom = roomManager.getRoom(roomCode);
    const currentEngine = roomManager.getGameEngine(roomCode);
    if (!currentRoom || !currentEngine) return;
    if (currentRoom.status !== 'RESULTS') return;
    try {
      startNewRound(currentRoom, currentEngine, io);
    } catch {
      io.to(`room:${roomCode}`).emit('game:error', 'Hra skončila — došly karty nebo nejsou aktivní hráči.');
    }
  }, 5_000);
}
```

**Uprav `isWinConditionMet()` pro multi-winner:**

```typescript
function isWinConditionMet(room: GameRoom, engine: GameEngine, result: RoundResult): boolean {
  const allWinnerIds = result.winnerIds && result.winnerIds.length > 0
    ? result.winnerIds
    : (result.winnerId ? [result.winnerId] : []);

  switch (room.winCondition ?? 'score') {
    case 'score':
      return allWinnerIds.some(
        wid => wid !== 'rando_cardrissian' && (result.scores[wid] ?? 0) >= room.targetScore
      );
    case 'rounds':
      return engine.roundNumber >= room.targetRounds;
    case 'time':
      return !!(room.gameStartedAt && Date.now() - room.gameStartedAt >= room.gameTimeLimit * 60_000);
  }
}
```

### Step 6: Přidej `hasVoted()` metodu do `GameEngine`

```typescript
hasVoted(playerId: string): boolean {
  return this.votes.has(playerId);
}
```

### Step 7: Spusť testy

```bash
npm test --workspace=packages/backend
```
Všechny testy musí projít.

### Step 8: Commit

```bash
git add packages/backend/src/socket/roundUtils.ts packages/backend/src/socket/gameHandlers.ts
git commit -m "feat: implement czar_is_dead voting handlers in roundUtils and gameHandlers"
```

---

## Task 5: Frontend — i18n překlady

**Files:**
- Modify: `packages/frontend/src/i18n/locales/cs.json`

### Step 1: Přidej `czarMode` sekci do `specialRules`

```json
"czarMode": {
  "label": "Karetní král",
  "classic": {
    "name": "Klasický",
    "desc": "Karetní král se střídá po směru hodinových ručiček."
  },
  "meritocracy": {
    "name": "Meritocracy",
    "desc": "Karetním králem se vždy stává vítěz minulého kola."
  },
  "god_mode": {
    "name": "God Mode",
    "desc": "Hostitel zůstává karetním králem po celou dobu hry."
  },
  "czar_is_dead": {
    "name": "Czar is Dead",
    "desc": "Není žádný karetní král — hlasují všichni. Vítěz získá nejvíce hlasů. Nikdo nesmí hlasovat pro sebe."
  }
},
"voteProgress": "Hlasovalo {voted} z {total} hráčů",
"skipVoting": "Dál nečekat",
"yourVoteCast": "Tvůj hlas byl odevzdán",
"voting": {
  "title": "Hlasování — vyber nejlepší odpověď!",
  "waiting": "Čekej na výsledky hlasování..."
}
```

Také uprav stávající `god_mode` a `meritocracy` — přesuň jejich popisy do `czarMode` sekce (jejich stávající klíče zachovej pro zpětnou kompatibilitu, jen přidej nové).

### Step 2: Commit

```bash
git add packages/frontend/src/i18n/locales/cs.json
git commit -m "feat: add czarMode and voting i18n keys to cs.json"
```

---

## Task 6: Frontend — SpecialRulesPanel + CzarModePanel

**Files:**
- Modify: `packages/frontend/src/components/SpecialRulesPanel.vue`
- Create: `packages/frontend/src/components/CzarModePanel.vue`

### Step 1: Odeber `god_mode` a `meritocracy` ze `SpecialRulesPanel.vue`

```typescript
// Nahraď RULES array:
const RULES: RuleInfo[] = [
  { id: 'rando_cardrissian', icon: '🎲' },
  { id: 'wheatons_law', icon: '🃏' },
  { id: 'rebooting_universe', icon: '♻️' },
  { id: 'high_stakes', icon: '💰' },
];

// Odeber CONFLICTS objekt (god_mode/meritocracy konflikty)
```

### Step 2: Vytvoř `CzarModePanel.vue`

```vue
<script setup lang="ts">
import type { CzarMode } from '@kpl/shared';
import { useI18n } from 'vue-i18n';

const props = defineProps<{
  modelValue: CzarMode;
  readonly?: boolean;
}>();
const emit = defineEmits<{ 'update:modelValue': [mode: CzarMode] }>();

const { t } = useI18n();

interface ModeInfo {
  id: CzarMode;
  icon: string;
}

const MODES: ModeInfo[] = [
  { id: 'classic', icon: '🔄' },
  { id: 'meritocracy', icon: '🏆' },
  { id: 'god_mode', icon: '👑' },
  { id: 'czar_is_dead', icon: '🗳️' },
];
</script>

<template>
  <div class="space-y-2">
    <p class="text-[10px] font-black uppercase tracking-[0.2em] text-slate-500 mb-3">
      {{ t('specialRules.czarMode.label') }}
    </p>
    <button
      v-for="mode in MODES"
      :key="mode.id"
      type="button"
      :disabled="readonly"
      @click="!readonly && emit('update:modelValue', mode.id)"
      :class="[
        'w-full text-left px-4 py-3 rounded-xl border transition-all',
        modelValue === mode.id
          ? 'bg-yellow-400/10 border-yellow-400/40 text-white'
          : 'bg-slate-900/40 border-white/5 text-slate-400',
        !readonly && 'hover:border-white/20 cursor-pointer',
        readonly && 'cursor-default',
      ]"
    >
      <div class="flex items-center justify-between gap-3">
        <div class="flex items-center gap-2 min-w-0">
          <span class="text-lg shrink-0">{{ mode.icon }}</span>
          <div class="min-w-0">
            <div class="text-sm font-bold truncate">
              {{ t(`specialRules.czarMode.${mode.id}.name`) }}
            </div>
            <div class="text-xs text-slate-500 mt-0.5 leading-snug">
              {{ t(`specialRules.czarMode.${mode.id}.desc`) }}
            </div>
          </div>
        </div>
        <div
          v-if="!readonly"
          :class="[
            'shrink-0 w-4 h-4 rounded-full border-2 transition-all',
            modelValue === mode.id
              ? 'bg-yellow-400 border-yellow-400'
              : 'border-slate-600',
          ]"
        />
        <span v-else-if="modelValue === mode.id" class="shrink-0 text-xs font-bold text-yellow-400 uppercase tracking-widest">
          {{ t('specialRules.active') }}
        </span>
      </div>
    </button>
  </div>
</template>
```

### Step 3: Commit

```bash
git add packages/frontend/src/components/SpecialRulesPanel.vue packages/frontend/src/components/CzarModePanel.vue
git commit -m "feat: add CzarModePanel component, remove god_mode/meritocracy from SpecialRulesPanel"
```

---

## Task 7: Frontend — CreateTableModal a RoomSettingsModal

**Files:**
- Modify: `packages/frontend/src/components/CreateTableModal.vue`
- Modify: `packages/frontend/src/components/RoomSettingsModal.vue`
- Modify: `packages/frontend/src/stores/roomStore.ts`

### Step 1: Uprav `roomStore.ts` — přidej `czarMode` do `updateSettings`

```typescript
import type { GameRoom, GameOverPayload, BlackCard, WhiteCard, AnonymousSubmission, RoundResult, SpecialRule, WinCondition, CzarMode } from '@kpl/shared';

// V updateSettings přidej czarMode:
async function updateSettings(settings: {
  // ...stávající...
  czarMode?: CzarMode;
}): Promise<{ error: string } | null> {
```

### Step 2: Uprav `CreateTableModal.vue`

**2a: Přidej import `CzarMode` a `CzarModePanel`**

```typescript
import type { SpecialRule, WinCondition, CzarMode } from '@kpl/shared';
import CzarModePanel from './CzarModePanel.vue';

const czarMode = ref<CzarMode>('classic');
```

**2b: Přidej `czarMode` do emit `create`**

```typescript
emit('create', {
  // ...stávající...
  czarMode: czarMode.value,
});
```

**2c: Přidej `CzarModePanel` do pravého sloupce (House Rules sekce)**

V template, před `<SpecialRulesPanel>`:

```html
<CzarModePanel v-model="czarMode" class="mb-6" />
<div class="border-t border-white/5 my-4" />
```

### Step 3: Uprav `RoomSettingsModal.vue`

**3a: Přidej import a stav**

```typescript
import type { GameRoom, SpecialRule, WinCondition, CzarMode } from '@kpl/shared';
import CzarModePanel from './CzarModePanel.vue';

const czarMode = ref<CzarMode>(props.room.czarMode ?? 'classic');
```

**3b: Přidej `czarMode` do `save()`**

```typescript
const err = await roomStore.updateSettings({
  // ...stávající...
  czarMode: czarMode.value,
});
```

**3c: Přidej `CzarModePanel` v template**

Stejně jako v CreateTableModal — před `<SpecialRulesPanel>`.

### Step 4: Uprav `lobbyStore.ts` nebo kde se volá `lobby:create` — přidej `czarMode`

Najdi v `lobbyStore.ts` nebo `HomeView.vue` kde se emituje `lobby:create` a přidej `czarMode` z přijatých settings.

### Step 5: Commit

```bash
git add packages/frontend/src/components/CreateTableModal.vue packages/frontend/src/components/RoomSettingsModal.vue packages/frontend/src/stores/roomStore.ts
git commit -m "feat: add czarMode selector to CreateTableModal and RoomSettingsModal"
```

---

## Task 8: Frontend — JudgingPhase pro czar_is_dead

**Files:**
- Modify: `packages/frontend/src/stores/roomStore.ts`
- Create: `packages/frontend/src/components/game/layouts/VotingLayout.vue`
- Modify: `packages/frontend/src/components/JudgingPhase.vue`

### Step 1: Přidej `game:vote`, `game:skipVoting`, `game:voteUpdate` do `roomStore.ts`

```typescript
// Přidej state:
const voteProgress = ref<{ votedCount: number; totalVoters: number } | null>(null);
const myVotedSubmissionId = ref<string | null>(null);

// V init() přidej:
socket.on('game:voteUpdate', (data) => {
  voteProgress.value = data;
});

// V game:roundStart handler: resetuj vote stav
voteProgress.value = null;
myVotedSubmissionId.value = null;

// Přidej funkce:
function castVote(submissionId: string) {
  socket.emit('game:vote', submissionId);
  myVotedSubmissionId.value = submissionId;
}

function skipVoting() {
  socket.emit('game:skipVoting');
}

// V cleanup(): přidej
socket.off('game:voteUpdate');
voteProgress.value = null;
myVotedSubmissionId.value = null;

// V return: přidej
voteProgress, myVotedSubmissionId, castVote, skipVoting,
```

### Step 2: Přidej `czarMode` computed do `roomStore.ts`

```typescript
const czarMode = computed(() => room.value?.czarMode ?? 'classic');

// V return: přidej czarMode
```

### Step 3: Vytvoř `VotingLayout.vue`

Komponenta pro fázi hlasování (czar_is_dead). Každý hráč vidí anonymní submissions a může hlasovat. Po hlasování se tlačítko deaktivuje. Po 60s se zobrazí "Dál nečekat".

Klíčové props:
- `blackCard: BlackCard`
- `submissions: AnonymousSubmission[]`
- `mySubmissionId: string | null` — ID vlastní submise (nelze hlasovat)
- `myVotedId: string | null` — ID za co jsem hlasoval
- `votedCount: number`
- `totalVoters: number`
- `secondsLeft: number`
- `revealedCount: number`

Events:
- `@vote(submissionId: string)`
- `@skipVoting()`

```vue
<script setup lang="ts">
import type { BlackCard, AnonymousSubmission } from '@kpl/shared';
import { useI18n } from 'vue-i18n';
import BlackCardDisplay from '../BlackCardDisplay.vue';
import SubmissionCard from '../SubmissionCard.vue';  // nebo inline

const { t } = useI18n();

const props = defineProps<{
  blackCard: BlackCard;
  submissions: AnonymousSubmission[];
  mySubmissionId: string | null;
  myVotedId: string | null;
  votedCount: number;
  totalVoters: number;
  secondsLeft: number;
  revealedCount: number;
}>();

const emit = defineEmits<{
  vote: [submissionId: string];
  skipVoting: [];
}>();
</script>

<template>
  <div class="flex flex-col gap-4 p-4">
    <!-- Header -->
    <div class="text-center">
      <p class="text-sm font-black uppercase tracking-widest text-yellow-400">
        {{ t('specialRules.voting.title') }}
      </p>
      <p class="text-xs text-slate-500 mt-1">
        {{ t('specialRules.voteProgress', { voted: votedCount, total: totalVoters }) }}
      </p>
    </div>

    <!-- Black card -->
    <BlackCardDisplay :card="blackCard" />

    <!-- Submissions -->
    <div class="space-y-3">
      <div
        v-for="(sub, i) in submissions"
        :key="sub.submissionId"
        v-show="i < revealedCount"
      >
        <!-- Submission cards display -->
        <div :class="[
          'rounded-xl border p-3',
          myVotedId === sub.submissionId
            ? 'border-yellow-400/50 bg-yellow-400/10'
            : 'border-white/10 bg-slate-900/40',
        ]">
          <div class="flex flex-wrap gap-2 mb-2">
            <div
              v-for="card in sub.cards"
              :key="card.id"
              class="text-sm text-white bg-white/10 rounded-lg px-3 py-2"
            >
              {{ card.text }}
            </div>
          </div>
          <button
            v-if="myVotedId === null && mySubmissionId !== sub.submissionId"
            @click="emit('vote', sub.submissionId)"
            class="w-full py-2 bg-yellow-400 text-black font-black text-xs uppercase tracking-widest rounded-lg hover:bg-yellow-300 transition-colors"
          >
            Hlasovat
          </button>
          <p
            v-else-if="myVotedId === sub.submissionId"
            class="text-xs text-yellow-400 font-bold text-center"
          >
            {{ t('specialRules.yourVoteCast') }}
          </p>
          <p
            v-else-if="mySubmissionId === sub.submissionId"
            class="text-xs text-slate-500 text-center"
          >
            Tvoje karty
          </p>
        </div>
      </div>
    </div>

    <!-- Skip voting button (after deadline) -->
    <button
      v-if="secondsLeft === 0 && myVotedId !== null"
      @click="emit('skipVoting')"
      class="w-full py-3 bg-slate-700 text-white font-black text-sm uppercase tracking-widest rounded-xl hover:bg-slate-600 transition-colors"
    >
      {{ t('specialRules.skipVoting') }}
    </button>
  </div>
</template>
```

### Step 4: Uprav `JudgingPhase.vue`

Přidej podmínku pro `czar_is_dead`:

```typescript
import VotingLayout from './game/layouts/VotingLayout.vue';

// Přidej mySubmissionId computed:
const mySubmissionId = computed(() => {
  // Najdi svoji submission podle myPlayerId (neznáme ji přímo, ale server ji anonymizoval)
  // Potřebujeme store ji při `game:judging` — přidej do roomStore `mySubmissionId`
  return roomStore.mySubmissionId;
});
```

> **Poznámka:** Server nepošle hráči jeho vlastní submissionId. Je potřeba ho sledovat v `roomStore` při odevzdání karet (`game:playCards`). Přidej do `roomStore` `mySubmissionId: ref<string | null>(null)` a nastav ho při odpovědi serveru (server musí odpovědět submissionId, nebo ho frontend odvodí ze `game:judging` — problém: je anonymizovaný).
>
> **Řešení:** Nejjednodušší je přidat nový server event `game:yourSubmissionId` posílaný jen konkrétnímu hráči po odevzdání karet, nebo server pošle submissionId v potvrzení `game:playCards` (ale ta není callback-based). **Alternativa:** Přidej callback do `game:playCards` který vrátí `submissionId`. Nebo: uložit submissionId před odesláním (frontend generuje random UUID = není možné bez úpravy backendu).
>
> **Doporučené řešení:** Uprav `game:playCards` handler na backendu aby emitoval zpět jen tomuto socketu `game:mySubmissionId: (id: string) => void` po úspěšném submitCards.

### Step 5: Přidej `game:mySubmissionId` event

**Backend (`gameHandlers.ts`):** Po úspěšném `engine.submitCards()` přidej:

```typescript
// Po result.ok check:
const sub = (engine as any).submissions.get(playerId);  // nebo přidej getSubmissionId metodu do GameEngine
if (sub) socket.emit('game:mySubmissionId', sub.submissionId);
```

**Lepší řešení — přidej `getSubmissionId()` do `GameEngine`:**

```typescript
getSubmissionId(playerId: string): string | null {
  return this.submissions.get(playerId)?.submissionId ?? null;
}
```

**Shared types — přidej event:**

```typescript
// ServerToClientEvents:
'game:mySubmissionId': (submissionId: string) => void;
```

**Frontend `roomStore.ts`:**

```typescript
const mySubmissionId = ref<string | null>(null);

// V init():
socket.on('game:mySubmissionId', (id) => {
  mySubmissionId.value = id;
});

// V game:roundStart: reset
mySubmissionId.value = null;

// V cleanup(): socket.off, reset
```

**Template v `JudgingPhase.vue`:**

```html
<VotingLayout
  v-if="roomStore.czarMode === 'czar_is_dead'"
  :blackCard="translatedBlackCard!"
  :submissions="translatedSubmissions"
  :mySubmissionId="roomStore.mySubmissionId"
  :myVotedId="roomStore.myVotedSubmissionId"
  :votedCount="roomStore.voteProgress?.votedCount ?? 0"
  :totalVoters="roomStore.voteProgress?.totalVoters ?? 0"
  :secondsLeft="secondsLeft"
  :revealedCount="revealedCount"
  @vote="roomStore.castVote"
  @skipVoting="roomStore.skipVoting"
/>
<CzarJudgingLayout
  v-else-if="roomStore.isCardCzar"
  ...
/>
<WaitingForCzarLayout
  v-else
  ...
/>
```

### Step 6: Commit

```bash
git add packages/frontend/src/stores/roomStore.ts packages/frontend/src/components/JudgingPhase.vue packages/frontend/src/components/game/layouts/VotingLayout.vue packages/shared/src/index.ts packages/backend/src/socket/gameHandlers.ts
git commit -m "feat: implement czar_is_dead voting UI - VotingLayout, roomStore updates, mySubmissionId tracking"
```

---

## Task 9: Výsledky pro více vítězů (czar_is_dead remíza)

**Files:**
- Modify: `packages/frontend/src/components/ResultsPhase.vue` (nebo ekvivalent)
- Modify: `packages/frontend/src/i18n/locales/cs.json`

### Step 1: Najdi ResultsPhase komponentu

```bash
# Zkontroluj:
packages/frontend/src/components/ResultsPhase.vue
```

### Step 2: Uprav zobrazení vítěze pro `winnerIds`

```typescript
// Přidej computed:
const winnerNames = computed(() => {
  const result = roomStore.roundResult;
  if (!result) return [];
  if (result.winnerIds && result.winnerIds.length > 0) {
    return result.winnerIds.map(id => {
      if (id === 'rando_cardrissian') return 'Rando Cardrissian';
      return roomStore.room?.players.find(p => p.id === id)?.nickname ?? id;
    });
  }
  return result.winnerId ? [result.winnerNickname ?? result.winnerId] : [];
});
```

### Step 3: Commit

```bash
git add packages/frontend/src/components/ResultsPhase.vue
git commit -m "feat: show multiple winners in ResultsPhase for czar_is_dead ties"
```

---

## Task 10: Finální testy a ruční ověření

### Step 1: Spusť všechny backend testy

```bash
npm test --workspace=packages/backend
```
Musí být všechny zelené.

### Step 2: Build backendu a frontendu

```bash
npm run build
```
Žádné TS chyby.

### Step 3: Ruční test v dev módu

```bash
npm run dev:backend &
npm run dev:frontend
```

1. Vytvoř místnost s `czarMode: czar_is_dead`
2. Přidej 2+ hráče
3. Spusť hru — ověř, že nikdo nemá isCardCzar=true
4. Všichni odevzdají karty
5. Fáze JUDGING — ověř, že se zobrazí hlasovací UI
6. Hráčům se zobrazí "Tvoje karty" u vlastní submise
7. Hlas → counter se aktualizuje
8. Po 60s → tlačítko "Dál nečekat"
9. Vítěz s nejvíce hlasy dostane +1 bod

### Step 4: Final commit

```bash
git add -p
git commit -m "feat: complete czar_is_dead implementation with voting UI and multi-winner results"
```

---

## Poznámky pro implementátora

- `god_mode` a `meritocracy` se nadále zobrazují v `SpecialRulesPanel` na LobbyPanel (readonly mód pro hráče) — uprav i `LobbyPanel.vue` aby zobrazoval `czarMode` ze `room.czarMode`, ne ze `specialRules`
- Backward compat: existující místnosti bez `czarMode` budou mít `undefined` → fallback na `'classic'` ve všech místech
- Snapshot/restore: `czarMode` se ukládá do `EngineSnapshot` — při restartu serveru se stav obnoví správně
- `rando_cardrissian` v czar_is_dead: Rando stále odevzdá karty, ale nemůže hlasovat (nemá playerId). Pokud Rando vyhraje, zobrazí se jako vítěz (implementace v `resolveVotes` to pokrývá)
